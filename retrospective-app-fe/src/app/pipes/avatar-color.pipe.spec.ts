import { AvatarColorPipe } from './avatar-color.pipe';

describe('AvatarColorPipe', () => {
  it('create an instance', () => {
    const pipe = new AvatarColorPipe();
    expect(pipe).toBeTruthy();
  });
});
